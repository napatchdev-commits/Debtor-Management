# 📗 คู่มือการเปลี่ยนฐานข้อมูลเป็น Google Sheets 100% (Google Sheets Database Setup Guide)

คู่มือนี้แนะนำขั้นตอนการสร้างตาราง Google Sheets และการนำ Web App URL ไปกรอกในระบบ Vercel เพื่อเปลี่ยนระบบให้ใช้ **Google Sheets เป็นฐานข้อมูล 100%** โดยไม่ต้องเสียค่าบริการใดๆ และสามารถเปิดดูตารางข้อมูลทั้งหมดบน Google Drive ได้โดยตรง

---

## 🛠️ ขั้นตอนการตั้งค่า 3 สเต็ปง่ายๆ

### สเต็ปที่ 1: สร้าง Google Spreadsheet บน Google Drive
1. เปิดเว็บไซต์ [Google Drive](https://drive.google.com) ของคุณ
2. กดปุ่ม **+ ใหม่ (+ New)** -> เลือก **Google สเปรดชีต (Google Sheets)**
3. ตั้งชื่อสเปรดชีต เช่น `ระบบชำระหนี้และหักค่าแรง`

---

### สเต็ปที่ 2: ติดตั้งสคริปต์อัตโนมัติ (Google Apps Script)
1. ในหน้า Google Sheets ให้ไปที่เมนูด้านบนเลือก **ส่วนขยาย (Extensions)** -> **Apps Script**
2. ลบโค้ดเริ่มต้นในหน้าต่างออกทั้งหมด
3. คัดลอกโค้ดทั้งหมดจากไฟล์ [`google_sheets_setup/GoogleSheetsScript.gs`](file:///a:/%E0%B8%8A%E0%B8%B3%E0%B8%A3%E0%B8%B0%E0%B8%AB%E0%B8%99%E0%B8%B5%E0%B9%89/google_sheets_setup/GoogleSheetsScript.gs) นำไปวางในหน้าต่าง Apps Script
4. กดปุ่ม **บันทึก (💾)**
5. กดปุ่ม **ทำให้ใช้งานได้ (Deploy)** มุมขวาบน -> เลือก **การทำให้ใช้งานได้ใหม่ (New deployment)**
6. ตรงสัญลักษณ์เฟือง ⚙️ ด้านซ้าย เลือกประเภทเป็น **เว็บแอป (Web app)**
7. กำหนดค่าดังนี้:
   - **คำอธิบาย (Description)**: `Debtor Management Database Engine`
   - **สิทธิ์การทำงานเป็น (Execute as)**: `ฉัน (Me)`
   - **ผู้มีสิทธิ์เข้าถึง (Who has access)**: `ทุกคน (Anyone)` *(สำคัญมาก: ต้องเลือกเป็น "ทุกคน" เพื่อให้ระบบหลังบ้านส่งข้อมูลได้)*
8. กดปุ่ม **ทำให้ใช้งานได้ (Deploy)** -> หากมีหน้าต่างขออนุมัติสิทธิ์ (Authorization) ให้กดอนุมัติสิทธิ์เข้าถึง
9. คัดลอก **URL ของเว็บแอป (Web App URL)** ที่ได้มา (ตัวอย่าง URL: `https://script.google.com/macros/s/AKfycbx.../exec`)

---

### สเต็ปที่ 3: นำ Web App URL ไปใส่ใน Vercel / .env
1. เข้าไปที่แดชบอร์ดโครงการของคุณใน [Vercel](https://vercel.com)
2. ไปที่ **Settings** -> **Environment Variables**
3. เพิ่มตัวแปรใหม่:
   - **Key**: `GOOGLE_SHEETS_WEBAPP_URL`
   - **Value**: `วาง URL ของเว็บแอปที่คัดลอกมาจากสเต็ปที่ 2`
4. กด **Save** แล้วกด **Redeploy** โครงการของคุณใน Vercel
5. เมื่อรีโหลดหน้าเว็บ ระบบจะสร้างแท็บตารางทั้ง 5 แท็บ (`users`, `debtors`, `jobs`, `debt_transactions`, `audit_logs`) ลงใน Google Spreadsheet ของคุณให้อัตโนมัติทันที!

---

## 📊 รายชื่อตารางข้อมูลใน Google Spreadsheet

เมื่อตั้งค่าเรียบร้อยแล้ว ใน Google Spreadsheet ของคุณจะมีแท็บตารางดังนี้:

| ชื่อแท็บตาราง (Tab Name) | รายละเอียดข้อมูล |
| :--- | :--- |
| **`users`** | บัญชีผู้ใช้งานระบบ (มี Admin เริ่มต้น: `admin` / รหัสผ่าน: `admin123`) |
| **`debtors`** | รายชื่อลูกหนี้ เบอร์โทร ยอดหนี้เริ่มต้น และสถานะชำระหนี้ |
| **`jobs`** | ประวัติการบันทึกงาน สถานที่จัดงาน จำนวนค่าแรง ยอดเบิก และยอดหักหนี้ |
| **`debt_transactions`** | ประวัติการตัดหักหนี้รายครั้ง ยอดหนี้ก่อนหัก และยอดหนี้คงเหลือหลังหัก |
| **`audit_logs`** | ประวัติการเข้าใช้งานและบันทึกกิจกรรมในระบบ |

---

> [!TIP]
> **คำแนะนำ**: คุณสามารถเปิดดูตาราง กรองข้อมูล หรือกดเมนู ไฟล์ -> ดาวน์โหลด เป็น `.xlsx` (Excel) บน Google Drive ได้ตลอดเวลาครับ!
